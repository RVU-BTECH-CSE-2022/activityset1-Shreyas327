#include<stdio.h>
int main(void)
{
  float length = 10.0, base=20.5;
  float area = 0.5*length*base;
  printf("The area of the triangle with length =%f and base=%f is %f\n",length,base,area);
  return 0;
}
